// import { createPost } from '../src/firebase.js';
import { showSignUp } from '../src/component/home';

describe('showSignUp', () => {
  it('is a function', () => {
    expect(typeof showSignUp).toBe('function');
  });
});

// describe('createPost', () => {
//   it('is a function', () => {
//     expect(typeof createPost).toBe('function');
//   });
// });
